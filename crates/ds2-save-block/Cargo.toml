[package]
name = "ds2-save-block"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
repository.workspace = true

# The policy module is plain arithmetic over five fields and builds everywhere, so its tests run on
# the machine the code is written on. Only the detour needs Windows.
[target.'cfg(windows)'.dependencies]
ds2-game-base.workspace = true
ds2-hook.workspace = true
ds2-rva.workspace = true

[lints]
workspace = true
